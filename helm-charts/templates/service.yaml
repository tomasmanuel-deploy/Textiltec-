apiVersion: v1
kind: Service
metadata:
  name: {{ include "prakash-billing-dashboard.fullname" . }}
  labels:
    {{- include "prakash-billing-dashboard.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "prakash-billing-dashboard.selectorLabels" . | nindent 4 }}
