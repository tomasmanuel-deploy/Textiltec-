Place your app icon PNG here as `icon.png`.

Requirements:
- Prefer a square image (1024x1024 recommended).
- Transparent background if possible.
- High-contrast for small sizes.

Build usage:
- macOS DMG builds will auto-generate `build/icons/icon.icns` from `assets/icon.png` via `npm run icons:mac`.